{% extends "layouts/admin.twig" %}

{% block title %} USUARIOS | SHOW {% endblock %}

{% block content %} 
<br>
    <!-- single product -->
	<div class="single-product mt-150 mb-150">
		<div class="container">
			<div class="row">
				<div class="col-md-5">
					<div class="single-product-img">
						<img src="assets/img/avaters/usuario.png" style="border-radius:50%"alt="">
					</div>
				</div>
				<div class="col-md-7">
					<div class="single-product-content">
						<h3>{{ user.nombre }} {{ user.apellidos }}</h3>
						<p>{{ user.email }}</p>
                        <p>{{ users_has_rol.id}}</p>
                        <br>
						<div class="single-product-form">
							<a href="{{ URL }}?controller=users&action=index" class="cart-btn"> Volver</a>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
	<!-- end single product -->
{% endblock %}